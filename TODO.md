# TODO

## Contact form: send email instead of opening mail client
- [ ] Replace `mailto:` redirect in `submitForm()` with EmailJS call.
- [ ] Add EmailJS initialization + template parameters (name, email, subject, message).
- [ ] Add success/error toast handling and disable button while sending.
- [ ] Update `index.html` only.
- [ ] Test form locally by submitting sample data.
- [ ] Deploy updated `index.html` to GitHub Pages and retest.

